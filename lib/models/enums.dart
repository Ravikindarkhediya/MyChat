enum MessageType {
  text,
  image,
  video,
  audio,
  file,
  location,
  contact,
  sticker,
  gif,
  voiceNote,
  deleted,
  custom
}

enum MessageStatus {
  sending,
  sent,
  delivered,
  read,
  failed,
  deleted
}
